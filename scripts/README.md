# Scripts

Diese Skripte sind bewusst „safe by default“ (keine Secrets, kein Remote-Deploy ohne explizite Inputs).

- `validate-overlay.sh`: Sanity-Check für `server-etc/openclaw/openclaw.overlay.json`
- `smoke-agent.sh`: prüft, ob die Workspace-Dateien vorhanden sind

